<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fitness Tracker</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background-color: #2c3e50; /* Dark Background for body */
            color: #f4f6f9;
            line-height: 1.6;
        }

        /* Header */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #1abc9c; /* Fresh accent color for the header */
            padding: 1rem 2rem;
            color: white;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        header .logo {
            font-size: 2rem;
            font-weight: 600;
            letter-spacing: 2px;
            color: #ffffff;
        }

        .nav-links {
            list-style: none;
            display: flex;
            gap: 1.5rem;
        }

        .nav-links a {
            text-decoration: none;
            color: white;
            font-size: 1.1rem;
            transition: color 0.3s ease, transform 0.3s ease;
        }

        .nav-links a:hover, .nav-links .active {
            color: #1abc9c;
            transform: scale(1.1);
        }

        /* Hero Section */
        .hero {
            text-align: center;
            padding: 4rem 2rem;
            background: linear-gradient(135deg, #e74c3c, #f39c12); /* Vibrant red to yellow gradient */
            color: white;
            border-radius: 15px;
            margin: 2rem auto;
            max-width: 1000px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        .hero h1 {
            font-size: 3rem;
            margin-bottom: 1.5rem;
            font-family: 'Poppins', sans-serif;
            letter-spacing: 2px;
        }

        .hero p {
            font-size: 1.3rem;
            margin-bottom: 2rem;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }

        .cta-button {
            display: inline-block;
            padding: 1rem 2.5rem;
            background-color: #ffffff;
            color: #e74c3c;
            text-transform: uppercase;
            font-weight: bold;
            border-radius: 30px;
            font-size: 1.2rem;
            letter-spacing: 1px;
            text-decoration: none;
            transition: background-color 0.3s ease, color 0.3s ease, transform 0.3s ease;
        }

        .cta-button:hover {
            background-color: #f39c12;
            color: white;
            transform: scale(1.1);
        }

        /* Fitness Image Section */
        .image-gallery {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            padding: 3rem 2rem;
            text-align: center;
            background-color: #fff;
            margin: 2rem auto;
            max-width: 1200px;
        }

        .image-card {
            border-radius: 12px;
            overflow: hidden;
            background-color: #fff;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .image-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
        }

        .image-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .image-card:hover img {
            transform: scale(1.1);
        }

        .image-card p {
            padding: 1rem;
            font-size: 1.1rem;
            color: #555;
        }

        /* Footer */
        footer {
            text-align: center;
            background-color: #34495e; /* Deep gray footer */
            color: white;
            padding: 1rem 0;
            font-size: 0.9rem;
            margin-top: 3rem;
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">FitTrack</div>
        <nav>
            <ul class="nav-links">
                <li><a href="index.html" class="active">Home</a></li>
                <li><a href="log.html">Activity Log</a></li>
                <li><a href="goals.html">Goals</a></li>
                <li><a href="profile.html">Profile</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <!-- Hero Section -->
        <section class="hero">
            <h1>Track Your Fitness Journey</h1>
            <p>Stay motivated and achieve your goals with FitTrack. Log your activities, monitor progress, and stay on track.</p>
            <a href="log.html" class="cta-button">Get Started</a>
        </section>

        <!-- Fitness Image Section -->
        <section class="image-gallery">
            <div class="image-card">
                <img src="https://via.placeholder.com/600x400/ff7f50/ffffff?text=Running" alt="Person running">
                <p>Track your runs and monitor your progress.</p>
            </div>
            <div class="image-card">
                <img src="https://via.placeholder.com/600x400/8e44ad/ffffff?text=Healthy+Meals" alt="Healthy meal">
                <p>Log your meals to stay on top of nutrition.</p>
            </div>
            <div class="image-card">
                <img src="https://via.placeholder.com/600x400/3498db/ffffff?text=Weight+Training" alt="Weight training">
                <p>Set strength goals and smash them!</p>
            </div>
        </section>
    </main>
    <footer>
        <p>&copy; 2024 FitTrack. Designed for fitness enthusiasts.</p>
    </footer>
</body>
</html>
